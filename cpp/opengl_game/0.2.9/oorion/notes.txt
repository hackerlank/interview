2) maths: дроби работают коряво
3) lexemize: отрицательные числа не поддерживаются
4) maths: операции % ^ не описаны и не поддерживаются
5) (FXD) maths: какие то глюки с переводом в ОПЗ int(a)=((7-3)*3)+(5*4); - ошибка, int(a)=((7-3)*3)+5*4 - Нет ошибки

сделать:
InitTextures
консоль
сделать if
вложенные условия
перенаправлять потоки с экрана в консоль и обратно
переделать ExecuteSystemFunction (рефакторинг)
подстановка переменных
поправить комменты вида // GUIContainer (GUIBaseControl)
BUG: 'echo('


офигенный баг с delete GUIVidget
